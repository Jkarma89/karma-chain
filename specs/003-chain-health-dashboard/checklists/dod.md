# Definition of Done 核对：功能 003 链状态与验证者网络实时监控面板

**核对日期**：2026-09-10
**依据**：项目宪法第十七条八项 + 本特性的 21 条成功判据

---

## 一、宪法第十七条八项

| # | 条件 | 状态 | 依据 |
|---|---|---|---|
| 1 | Specification 满足 | ✅ | 37 条 FR / 21 条 SC；`/speckit-analyze` 的 13 项 finding 全部处理（见下「分析轮修正」） |
| 2 | Constitution 满足 | ✅ | plan.md 的 20 条逐条核对 + **Gate 三查**；唯一的 CRITICAL（第四条要求的安全分析）已由 `security-probe-tx.md` 产出 |
| 3 | 测试通过 | ✅ | 单元 **456/456**、集成 **105 通过 / 3 跳过**（皆 002 既有）、面板 e2e **14/14**、`devnet-verify` **14/14**、`render:check` 10/10、秘密扫描 0 fail |
| 4 | 安全评估完成 | ✅ | `security-probe-tx.md`：四项例外条件、四个泄漏面、失败路径逐条、以及「无鉴权 + 会写链的端点」这个**组合**的风险边界 |
| 5 | 文档更新 | ✅ | `docs/devnet.md` §10（起法 / 三档表 / 两个余量 / 十场景读法 / 四不做 / 排障）、ADR-0010、`docs/adr/README.md` 登记、README 命令表 |
| 6 | 可观测性完成 | ✅ | **本特性即宪法第九条的落地**：逐节点状态/高度/peers/边界可见，异常按四类分组并各带处置方向 |
| 7 | 部署配置完成 | ✅ | `scripts/devnet-dashboard.{sh,ps1}` 成对；复用既有镜像，**未改** compose / Dockerfile / 节点标志 |
| 8 | 无已知严重回归 | ✅ | 002 的既有判据全部复跑通过；对 002 代码的两处追加各有回归断言（`node-status-genesis.test.mjs`） |

---

## 二、21 条成功判据

| SC | 内容 | 状态 | 实测依据 |
|---|---|---|---|
| SC-001 | 5/5 → 100% 正常，写明还可容忍 1 个 | ✅ | `dashboard-detection` 前置用例；真值表第 1 行 |
| SC-002 | 停 1 个 → ≤10 秒 80% 零余量，交易仍能确认 | ✅ | **实测 2942 ms / 2960 ms**；同时刻交易确认 748→749 |
| SC-003 | 停 2 个 → ≤10 秒 60% 已停止，交易无法确认 | ⚠️ **需两台机器** | 自动化 `dashboard-stopped-tier` 已就位（跨机带说明跳过）；档位判定由真值表第 6/8/10 行覆盖；**"交易确实无法确认"这一半事实待人工验**（quickstart 场景 C） |
| SC-004 | 恢复后 ≤10 秒回 100%，报警自消 | ✅ | `dashboard-detection` 第 3/4 用例 |
| SC-005 | 单链路故障 → 本机视角不可达，100% 不报警 | ✅ | `dashboard-link-fault`：l1-2 地址覆盖到黑洞后标「本机视角不可达」，健康度 100%，撤销后回 `healthy/768` |
| SC-006 | 观察者失明 → 失去观测能力，不出现"链已停止" | ✅ | `dashboard-server` 集成用例 + `dashboard-blindness` 单元 + `dashboard-copy` 的禁止词断言 |
| SC-007 | 两个 Primary 全停 → L1 仍 100% | ⚠️ **需两台机器** | 自动化 `dashboard-primary-loss` 已就位（跨机带说明跳过）；**FR-013 的静态守卫已补**（`dashboard-boundaries` 禁 `/ext/health`），不再只靠这个会跳过的 e2e |
| SC-008 | 空闲不报警 | ✅ | `dashboard-idle`：60 个样本档位/百分比/高度/异常条目**逐样本完全相同** |
| SC-009 | 节点与边界与 protocol.json 一致；切 local 无需改代码 | ✅ | `dashboard-nodes` 10 项 |
| SC-010 | catching-up 显示落后量且不改档位 | ✅ | `dashboard-detection` 第 3 用例（恢复过程只出现 `zero-margin`/`normal`，无 `stopped`） |
| SC-011 | 五台创世一致则不报分叉，任一不一致则显目报出 | ✅ | `dashboard-genesis-parity`：五台全部匹配 `0x19cfde1f…92ed`（跨 amd64/arm64）；不一致分支由 `dashboard-fork` 单元覆盖 |
| SC-012 | 精简视图不含内部事实 | ✅ | `dashboard-public-view` **三层守卫**（结构 + 内容扫描 + **行为探针**） |
| SC-013 | 数据落后 ≤10 秒且采集时刻可见 | ✅ | `dashboard-interval`：12 秒连续采样，年龄最大 **5724 ms** |
| SC-014 | 未见过面板的人 1 分钟内答对两个问题 | ⚠️ **待人工** | 需要一名此前未见过面板的人。见下「待人工完成」 |
| SC-015 | 002 既有验收无回归 | ✅ | `devnet-verify` 14/14、创世哈希不变、单元 456/456 |
| SC-016 | 非 5 验证者拓扑下档位仍正确 | ✅ | 真值表 4b 节：n=9/f=2 时 8/9=89% 仍 `normal`、7/9=78% `zero-margin`、6/9=67% `stopped` |
| SC-017 | 面板自身不产生区块 | ✅ | `dashboard-readonly`：24 轮采集，高度 755→755；另有源码静态守卫 |
| SC-018 | 探活结果与档位一致 | ✅（正向） | `dashboard-probe`：确认于区块 752、1011 ms；单飞守卫生效。反向（stopped 档）随 SC-003 待验 |
| SC-019 | 分批启动全程不报"已停止" | ⚠️ **待人工** | 档位判定由真值表第 7/9 行 + `dashboard-blindness` 的 P2 优先级覆盖；**"全网分批启动"这一过程**需要五台机器配合（quickstart 场景 J） |
| SC-020 | 无未分类异常 | ✅ | `dashboard-incidents-complete`：9 状态 × 2 离线语义 × 5 档位 × 2 分叉**穷举** |
| SC-021 | 各节点说法不一致时呈现各自数值 | ✅ | `view-observer.mjs` 的 `renderDisagreement`；判定层不合并（`deriveTier` 的输入里没有"合并后的高度"这个概念） |

**17 项已达成 ✅ / 4 项待人工或需多机 ⚠️ / 0 项失败。**

---

## 三、`/speckit-analyze` 轮的修正（2026-09-10）

分析查出 **1 CRITICAL + 5 HIGH + 5 MEDIUM + 2 LOW**，全部处理。其中三项是**"守卫不会变红"**
这一形态，值得单列：

| finding | 问题 | 修正 |
|---|---|---|
| **D1**（CRITICAL） | 宪法第四条要求 Key Management 的修改必须做安全分析；本特性新增了一条密钥使用路径（探活读私钥签名），却无任何任务产出该分析 | 新增 T032a → `security-probe-tx.md` |
| **C1** | FR-013（不得用综合健康位）唯一的守卫是 `dashboard-primary-loss`，而它在跨机形态下**必然跳过** —— 实际部署上这条规则一个长期运行的守卫都没有 | 新增 T013a：静态禁止面板源码出现 `/ext/health`，无论 e2e 是否跳过都执行 |
| **C2** | FR-008/FR-020/SC-006 的判据本质是"文案里不得出现某些话"，而原先只有实现任务、**零测试** | 文案抽成零依赖纯模块 `copy.mjs` + T026a 逐档位断言必含/禁止短语 |
| **I1** | FR-005 原写「计入在线的验证者数」—— 那正好对应既有 `countsAsOffline`，而它含 `bootstrapping`。照字面实现会把「1 健康 + 4 引导中」算成 100% | 改为「**参与共识**的验证者数」，并就地记录后果 |
| **I2** | FR-007 原写「三档且三档互斥」，但设计有五个互斥状态 | 改为「三个健康度档位 + 两个非健康度状态，五者互斥」 |
| **I3** | `app.mjs` 被 14 条任务横跨全部六个故事触及，与文档自己的串行规则冲突 | 前端拆成 `app.mjs`（只做轮询/路由/装配）+ 六个 `view-*.mjs`，每个故事只碰自己那一个 |

---

## 四、实现期发现并修掉的缺陷

**这一节记的是"如果没做这一步就不会发现"的东西。**

| # | 缺陷 | 怎么发现的 | 性质 |
|---|---|---|---|
| 1 | `docker run -it` 在 stdin 非终端时直接失败（`cannot attach stdin to a TTY-enabled container`） | 冒烟测试（后台启动面板容器） | 人手工敲命令永远看不到；后台/CI 调用必炸 |
| 2 | 基准创世哈希读不到时，`String(null)` 参与比较 → **每个节点都被判成创世不符** | `dashboard-fork.test.mjs` 的「基准缺失时全部为 null」 | 一个仓库文件读不到会变成一场五机分叉警报 |
| 3 | `localVictimSkip` 被当成判定用（它只生成说明文本）→ e2e 在靶子充足时也恒跳过 | 第一次跑 `dashboard-detection` 时它跳过了 | 静默跳过 —— 正是本特性一路在防的形态 |
| 4 | `starting` 档文案写了「也不是"须处置"」，被子串守卫抓住 | `dashboard-copy.test.mjs` | **改文案而非放宽守卫**：否定式措辞对子串检查天生不友好 |
| 5 | 入口脚本注释里复述了节点端口区段；测试夹具写死 `21660`/`192.168.1.3` | 002 的 `no-hardcode` 守卫 | 守卫是对的，**未登记任何豁免** |
| 6 | 我给 003 加的 `\b5\b` 规则抓到 `order: 5`（视图排序号）—— 假阳性 | 自己的守卫 | 删掉噪音规则，指向真正管事的 n=9/f=2 行为守卫。噪音会让人开始忽略红灯 |
| 7 | T005 的 `n=9/f=2` 判据我写错了（从 n=5 想当然外推「8/9 = 零余量」，实为 `normal`） | 写测试时逐行算 | 已修测试 + tasks.md，并在契约新增 4b 节记录这次外推错误 |
| 8 | T020 在 Phase 2 却要渲染到 US1 才创建的 `view-health.mjs` | 视图归属的机械核对 | 阶段依赖倒置，已拆开 |

另有两处对任务的**偏离**已就地记录：T031 的新鲜度落在 `app.mjs` 顶栏（它是整页属性）、
T015 加了只为测试留的接缝（TTL 判定不能测就等于没守卫）。

---

## 五、待人工完成（无法在单台机器上做）

| 项 | 需要什么 | 对应 |
|---|---|---|
| **V-01 其余四台** | 在 win-2 / ubuntu-1 / ubuntu-2 / ubuntu-3 上各跑一次 `devnet-dashboard`，确认宿主浏览器能访问 | T072。Linux 与 Windows 的端口发布路径不同，不能由 win-1 推断 |
| **SC-003 / V-04** | 两台机器上各 `devnet-node kill` 一个验证者，确认 60% 档 + 交易无法确认 | quickstart 场景 C |
| **SC-007** | 在 ubuntu-1 与 ubuntu-2 上各停一个 Primary | quickstart 场景 F |
| **SC-019** | 五台全停后逐台启动，全程观察面板 | quickstart 场景 J |
| **SC-014** | 一名此前未见过本面板的人 | T076。答不上就改呈现，**不改判据** |
| 场景 E 的物理断网 | 拔掉观察者的网线 | V-02 已用黑洞地址在观测层等价验证过 |
| 场景 G / H 的完整窗口 | 空闲 10 分钟、只读 30 分钟 | 自动化各覆盖 60 秒窗口 |

---

## 六、结论

**代码与自动化验证部分已完成**：73 / 84 任务，17 / 21 条 SC 达成，全部自动化判据通过，
零新增依赖，002 运行时制品零改动。

**剩余 11 个任务全部是需要多机配合或人工判断的验收步骤**（T071–T076 的一部分），
不含任何未写完的代码。
