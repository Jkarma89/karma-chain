# 架构决策记录（ADR）

宪法第十四条要求任何重要架构决策都必须记录：决定了什么 / 为什么 / 考虑过哪些替代方案 / 有什么影响 / 以后如何迁移。

| # | 决策 | 状态 | 日期 |
|---|---|---|---|
| [0001](0001-avalanche-l1-and-evm.md) | 以 Avalanche L1 + EVM 作为链层 | 已接受 | 2026-09-01 |
| [0002](0002-avalanche-cli-toolchain-and-versions.md) | 用（维护模式的）Avalanche CLI 编排本地网络，锁定全部组件版本 | 已接受（含技术债） | 2026-09-01 |
| [0003](0003-chain-identity-and-dev-security-boundary.md) | 链身份（Chain ID 20189 / 主网预留 20188）与开发密钥安全边界 | 已接受 | 2026-09-01 |
| [0004](0004-five-validator-local-topology.md) | 本地网络采用 5 个 L1 验证者 + 2 个主网节点 | 已接受 | 2026-09-01 |
| [0005](0005-permissionless-deployment-model.md) | 采用无许可模型（开放公链生态） | 已接受 | 2026-09-03 |
| [0006](0006-windows-failure-domain-autostart.md) | Windows 故障边界不做开机自启，接受人工恢复（候选 2 已被 `WSL_E_LOCAL_SYSTEM_NOT_SUPPORTED` 否决） | 已接受 | 2026-09-07 |
| [0007](0007-failure-domain-independence.md) | 故障边界独立性的依据与已知限制。2026-09-07 取证发现声明的 5 个边界只对应 **2 台物理机**（全部测试皆绿，唯一破口是共用的 MAC）；2026-09-08 硬件更换为 **5 台独立物理机**并重新清点通过，整域失效容忍**为 true** 且已由 30 分钟窗口实证 | 已接受（含修正） | 2026-09-07（2026-09-09 更新现状） |
| [0008](0008-runtime-without-orchestration-cli.md) | 运行时脱离编排工具：一节点一容器直接运行 avalanchego（取代 ADR-0002 的运行时部分） | 已接受 | 2026-09-07 |
| [0009](0009-chain-identity-as-second-class-fact.md) | 建链制品是"第二类事实"，不由 protocol.json 派生 | 已接受 | 2026-09-07 |

## 待决策（记录在案，避免遗漏）

这些问题已被识别但尚未决策，将在相应功能的规格阶段处理：

| 议题 | 为何重要 | 相关 |
|---|---|---|
| **Gas 与费率参数的安全评估** | 无许可链上费率是抗滥用的**唯一**机制；当前值为 Subnet-EVM 默认，未针对开放网络分析过。**且上线后基本改不动**：`upgrade.json` 不能改 feeConfig，唯一的运行时途径 `feeManagerConfig` 预编译已被 ADR-0005 否决，剩下只有"重新部署整条链"。等于主网上线时的取值近乎永久 | ADR-0005 影响节。**主网上线的硬前置**，开发网阶段不必动 |
| ~~**验证者模型的去中心化路径**~~ | **已由功能 002 处理，2026-09-09 完成**：验证者从"单容器内 5 个进程"变为**实际分布在 5 台独立物理机上**的崩溃自愈独立节点，编排工具退出运行时。任一边界整体失效时链继续出块（30 分钟窗口 30/30 笔确认）。**注意这解决的是物理冗余与可恢复性，不是权限去中心化** —— 验证者集合仍由 PoA 合约的单一 owner 管理，那部分留在"主网代币经济学"与未来的治理规格里 | ADR-0004、[0007](0007-failure-domain-independence.md)、[0008](0008-runtime-without-orchestration-cli.md) |
| **主网代币经济学** | 初始分配、是否可增发（当前不可）、手续费处理（当前销毁）需独立规格 | ADR-0003、ADR-0005 |
| **Chain ID 注册** | 20188/20189 已核实未占用，但正式发布前需向 ethereum-lists/chains 提交注册 | ADR-0003 |
| ~~**面向第三方的公开接入信息**~~ | **已完成**（2026-09-03）：`docs/public/chain-info.json` + `developer-quickstart.md`，由 `protocol.json` 生成、有漂移测试，且每一项都可用公开 RPC 独立求证。karma-sc 已改为纯第三方消费者并由测试强制 | 见 `tools/protocol/render-chain-info.mjs` |
| **公开可访问的 RPC 端点** | 上一条把"知识"补齐了，但**连接**还没有：公布的端点是 `http://127.0.0.1:8545/…`，只对本机有效。局域网协作可临时换成宿主 IP；真正的外部开发者需要一个公开可达的端点。这牵扯域名、TLS、限流、滥用防护，以及 avalanchego 的 `--http-allowed-hosts` 只放行 localhost/IP 字面量（反向代理需改写 Host 或调整该配置） | 一有外部协作者即需要；主网必需 |
| **官方合约 ABI 的发布** | `chain-info.json` 的 `contracts` 目前只有地址、**没有 ABI**。第三方要与官方合约交互，光有地址不够。有业务合约后需把 ABI（或其获取方式）纳入公开接口，并同样做成生成物以防漂移 | 第一个业务合约上链时 |
| **区块浏览器** | 第三方看不到交易、无法验证合约源码、排查失败只能靠 RPC。开放生态下它还是防诈骗的关键一环（源码验证、诈骗标记，见 ADR-0005 下游要求） | 主网前基本必需 |
| **合约升级策略** | 官方合约是否使用代理模式，须在编写合约前决定（宪法第六条） | 智能合约功能 |

## 写新 ADR

沿用现有编号顺序，文件名 `NNNN-短横线标题.md`，包含：状态 / 日期 / 相关宪法条款 → 决定了什么 → 为什么 → 考虑过的替代方案 → 影响 → 迁移。写完后更新本表。
