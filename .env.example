# KarmaChain 本地开发网络 — 可选的宿主机覆盖项
# 复制为 .env 后生效（.env 已被 .gitignore 忽略）。
#
# 重要：下列默认值的唯一权威定义在 blockchain/protocol.json（宪法第十六条）。
# 这里的覆盖只改变"宿主机如何映射/等待"，不改变链身份（Chain ID、Network ID、代币）。

# 宿主机映射的 RPC 端口（默认 8545 = protocol.json → endpoints.hostRpcPort）
# 仅在 8545 被其他进程占用时修改；启动摘要会打印实际生效值。
# KARMACHAIN_RPC_PORT=8545

# 启动就绪超时（秒，默认 300 = SC-001 的 5 分钟目标；首次构建镜像不计入）
# KARMACHAIN_STARTUP_TIMEOUT=300
