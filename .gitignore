# ---- 运行时数据（绝不入库）----
# 本地开发网络的链数据、快照、日志全部位于 Docker 命名卷；若被挂载到工作区也一并忽略
.devnet/
.avalanche-cli/
.avalanchego/
.tmpnet/
*.db
*.log

# ---- Node ----
node_modules/
npm-debug.log*

# ---- 本地环境覆盖（示例文件 .env.example 保留）----
.env
.env.local

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
.DS_Store
Thumbs.db

# ---- 安全：任何非 DEVELOPMENT ONLY 目录下的密钥文件 ----
# 允许的公开开发密钥仅位于 blockchain/accounts/ 与 blockchain/validators/dev/（见宪法第四条 v1.1.0 例外条款）
*.pem
*.key
# **功能 005 起不再放行这个目录下的 .key。** 创世那五个（node-1…node-5 与两个 primary）
# 已在索引里，git 的忽略规则**不影响已跟踪的文件**，所以它们照旧跟踪；
# 而创世**之后**加入的验证者，私钥必须在目标机器上生成并留在本机 ——
# 只有公开材料（NodeID / BLS 公钥 / 三个 sha256 指纹）进 blockchain/deployment.json。
# 原先那条 `!blockchain/validators/dev/**/*.key` 会让新节点的私钥被 git add -A 直接提交。
